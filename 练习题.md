# 数据分析课后练习题

基于 `analyzer.py` 实战项目，检验你对 pandas + matplotlib 的掌握程度。

---

## 第1题：新增"月度订单数量趋势"图 ⭐

**任务**：在 `ProcurementAnalyzer` 类里新增一个 `order_count_trend()` 方法，画一条**折线图**，X 轴是月份，Y 轴是每个月的订单数量。

**提示**：
- 用 `groupby("月份")` 分组后，对任意列做 `count()` 就能拿到每月订单数
- 用 `ax.plot()` 画折线，记得加 `marker="o"` 标记数据点
- 别忘了在 `run_all()` 里调用新方法

---

## 第2题：找出"异常高价订单" ⭐⭐

**任务**：新增 `find_expensive_orders()` 方法，找出**单笔金额超过该类别平均金额 3 倍**的订单，打印出来，并保存到 `charts/expensive_orders.csv`。

**提示**：
- 先 `groupby("产品类别")["总金额"].transform("mean")` 算出每个类别的平均金额
- 用布尔索引筛选 `总金额 > 平均金额 * 3`
- `to_csv()` 保存结果

---

## 第3题：供应商分析——改"Top10"为"Bottom5" ⭐⭐

**任务**：把 `supplier_analysis()` 方法复制一份，改名为 `supplier_bottom5()`，画出**采购金额最少的 5 个供应商**的水平柱状图，颜色用橙色系。

**提示**：
- `sort_values(ascending=True)` 取 `head(5)` 就是最小的
- 颜色可以改成 `colors=["#e67e22", "#f39c12", ...]`

---

## 第4题：散点图——数量 vs 金额 ⭐⭐⭐

**任务**：新增 `quantity_vs_amount()` 方法，画一张**散点图**，X 轴是"数量"，Y 轴是"总金额"，每个点代表一笔订单，按产品类别用不同颜色区分。

**提示**：
- 用 `ax.scatter(x, y, c="颜色", label="类别名")` 画散点
- 需要分别取出每个类别的数据，分别画点
- 最后加 `ax.legend()` 显示图例

---

## 第5题：数据导出——生成 Excel 汇总表 ⭐⭐⭐

**任务**：新增 `export_summary()` 方法，把三个汇总表写入一个 Excel 文件（`charts/summary.xlsx`），三个 sheet 分别是：
- "月度汇总"：每月订单数、总金额、平均单价
- "类别汇总"：每类订单数、总金额、平均金额
- "供应商汇总"：每个供应商订单数、总金额

**提示**：
- 需要安装 `openpyxl`：`pip install openpyxl`
- 用 `pd.ExcelWriter` 创建 Excel 写入器
- 每个 DataFrame 用 `to_excel(writer, sheet_name="xxx")` 写入不同 sheet

---

## 做题方式

建议按照**1→2→3→4→5**的顺序做，每题写完后运行 `python main.py` 看效果。

遇到问题随时问我，我会帮你分析而不是直接给答案——这才是练习的意义 😄
